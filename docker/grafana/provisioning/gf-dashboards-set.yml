apiVersion: 1
providers:
  - name: ${GF_DASHBOARDS_SET}
    orgId: 1
    type: file
    disableDeletion: true
    editable: true
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: "/opt/grafana/dashboards/${GF_DASHBOARDS_SET}"
      foldersFromFilesStructure: true